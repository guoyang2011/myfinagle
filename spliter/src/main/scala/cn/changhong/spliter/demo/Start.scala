package cn.changhong.spliter.demo



//   (Source.fromURL(new URL("http://apps.wandoujia.com/api/v1/apps?tag=相机&max=60&start=100")).getLines().foreach(println))
//  val doc=Jsoup.parse(new URL("http://www.wandoujia.com/apps"),1000)
//  val li=doc.select("li.parent-cate")
//  li.toArray()foreach{e=>println(e)}
//  val it=li.iterator()
//  while(it.hasNext){
//    val e=it.next()
//    println("\n\n")
//    val ems=e.select("a[href]")
//    val eit=ems.iterator()
//    while(eit.hasNext){
//      val eits=eit.next()
//      println(eits.attr("href")+","+eits.attr("title"))
//    }
//  }
}


ad,snippet,changelog,extra,apks,extensionPacks,cooperator,alias,selectedExtensionPack,downloadCount,banner,
reputationScore,installedCountStr,itemStatus,defaultVersionCode,negComments,categories,detailParam,updatedDate,
downloadCountStr,description,commentsCount,developer,likesRate,screenshots,exclusiveBadge,packageName,
likesCount,imprUrl,title,publishNoticeBean,appType,publishDate,broadTerms,editorComment,dislikesCount,adType,
latestApk,posComments,adPosition,installedCount,tagline,_id,iconsStr,tags,id,award,icons,stat,screenshotsStr
